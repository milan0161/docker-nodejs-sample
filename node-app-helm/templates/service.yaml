apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: node-app-service
spec:
  selector: 
    app: node-app
  ports:
    - port: 443
      name: https
      protocol: TCP
      targetPort: 3000

